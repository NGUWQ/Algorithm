树的特点:既能像链表一样快速的插入和删除，又能像有序数组一样进行快速的查找
树中每个节点(除了叶子节点)除了包含数据之外，还包含指向其他节点的引用
树的层次实际上是无限的直到溢出为止

非平衡树:大部分的节点再根的一边或者另一边

常见的树的时间复杂度位:O(log以2为底N为对数) 大O表示法中为:O(logN)

用数组表示树时：下标0为树的根,其他下标按顺序从左到右排列
设节点的索引值为index
其左节点为：2*index+1
其右节点为：2*index+2
其父节点为：(index-1)/2

二叉搜索树中,所有A节点左边的子孙节点关键字的值都比A小,所有A节点右边的子孙节点关键字的值都比A大.